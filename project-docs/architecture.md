# Architecture

The architecture follows a three-phase pipeline: [initialization][int], **bundling**, and **compilation**. Each phase has clear boundaries and responsibilities.

<!-- markdownlint-disable MD053 -->

[int]: initialization.md
