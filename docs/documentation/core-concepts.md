# Core Concepts

This document introduces the fundamental concepts underlying the susee bundler architecture. It provides a high-level understanding of how susee processes TypeScript source files into distributable library artifacts.

## The Three-Stage Build Pipeline

The susee build process executes in three sequential stages, each transforming data through a distinct phase of processing:
